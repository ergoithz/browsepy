language: python
python:
  - "2.7"
  - "pypy"
  - "3.3"

install:
  - pip install coveralls
  - python setup.py install

script:
  - make coverage

after_success:
  - coveralls

notifications:
  email: false
